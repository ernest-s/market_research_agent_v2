https://dev-27pe1ffaoilblt25.us.auth0.com/v2/logout
Command to create node_modules: npm install


To create & Start Postgres container
docker run --name qual-postgres \
  -e POSTGRES_USER=postgres \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_DB=qual_db \
  -p 5432:5432 \
  -d postgres:15


npm install
npx prisma generate
npx prisma migrate dev


# Start Docker from terminal
docker start qual-postgres


# Access Prisma Studio
npx prisma studio

# Single session enforecement
Add this block of code for all authenticated pages

const sessionId = req.cookies.get("app_session_id")?.value;
const session = await requireSession(sessionId);

if (!session) {
  return NextResponse.json({ error: "Unauthorized" }, { status: 401 });
}

Please refer src/app/api/user/profile/route.ts file for example usage

# Right now there are two folders 'frontend' and 'services'
# If a new folder is added to the root in future, update package.json in the root folder